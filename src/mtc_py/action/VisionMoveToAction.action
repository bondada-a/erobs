# Goal
int32 tag_id                # ArUco marker ID to detect
float64 timeout             # Detection timeout in seconds (default: 5.0)
string poses_json           # Pose definitions from task (unused for vision)
---
# Result
bool success
string error_message
---
# Feedback
